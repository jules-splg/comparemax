{
  "name": "comparemax-price-updater",
  "version": "1.0.0",
  "private": true,
  "description": "Automatic price updater for CompareMax — runs 3x/day via GitHub Actions",
  "scripts": {
    "update-prices": "node scripts/update-prices.js",
    "update-prices:dry": "node scripts/update-prices.js --dry-run"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
